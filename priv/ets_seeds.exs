# Script for populating ETS storage with seed data
# Run it as: mix run priv/ets_seeds.exs
#
# Note: This starts a separate application instance.
# For production, consider seeding on application startup.

ChimeraCms.EtsSeeds.run()
